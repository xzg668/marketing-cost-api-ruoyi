-- =============================================================================
-- V97: 移除 U9 料品主档页面的差异报告与手工同步权限
-- -----------------------------------------------------------------------------
-- U9 料品主档以导入/未来接口接入数据为准，页面不再提供 raw 与主表差异报告、
-- 也不再提供手工勾选同步入口，避免形成无效操作链路。
-- =============================================================================

SET NAMES utf8mb4;

DELETE FROM sys_role_menu WHERE menu_id IN (40439, 40440);
DELETE FROM sys_menu WHERE menu_id IN (40439, 40440);

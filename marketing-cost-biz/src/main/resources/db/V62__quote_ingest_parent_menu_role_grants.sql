-- =============================================================================
-- V62: 报价单接入父级菜单授权修复
-- -----------------------------------------------------------------------------
-- 背景：
--   V60 给 BU_DIRECTOR / BU_STAFF 授权了报价单导入、报价单接入、接入流水等
--   子菜单，但漏授父级 menu_id=200“报价需求”。动态菜单树需要父级目录也在
--   角色菜单集合内，否则 panxh/hfy 等非 admin 账号看不到整个报价需求目录。
-- =============================================================================

SET NAMES utf8mb4;

INSERT IGNORE INTO sys_role_menu (role_id, menu_id)
VALUES
  (1, 200),
  (10, 200),
  (11, 200);

-- V62 结束

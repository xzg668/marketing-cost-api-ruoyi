-- =============================================================================
-- V117: 净损失率配置 company 历史字段默认值
-- -----------------------------------------------------------------------------
-- 新的报价净损失率配置按 年度 + 产品料号/产品型号 匹配，不再维护公司维度。
-- 但旧表 company 字段仍为 NOT NULL，这里给默认空字符串，避免导入/手工新增失败。
-- =============================================================================

SET NAMES utf8mb4;

ALTER TABLE lp_quality_loss_rate
  MODIFY company VARCHAR(80) NOT NULL DEFAULT '' COMMENT '历史公司字段，新净损失率口径不参与匹配';
